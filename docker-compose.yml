services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: habits
      POSTGRES_USER: habits
      POSTGRES_PASSWORD: habits
    ports:
      - "5432:5432"
    volumes:
      - habits_data:/var/lib/postgresql/data

volumes:
  habits_data:
